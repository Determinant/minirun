---
- name: fetch the remote files
  block:
    - synchronize:
        archive: true
        src: "{{ testbed }}"
        dest: "{{ local_dir }}"
        mode: pull
  vars:
    local_dir: "{{ (work_dir, 'remote', nid) | path_join }}"
